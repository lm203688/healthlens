# HealthLens 全面评估与自动化补位方案

> 评估日期：2026-08-04 | 评估基准：代码实测 + 线上探测 + 运行日志，非文档宣称
> 扫描范围：healthlens/(132 py) · auto-pipeline/ · reports/ · 中医/ · healthlens-auto-tasks/

---

## 零、先看这一条

**healthlens.cc 当前处于宕机状态。**

```
https://healthlens.cc            HTTP 530
https://healthlens.cc/health     HTTP 530
https://healthlens.cc/sitemap.xml HTTP 530
```

530 = Cloudflare 回源失败，即源站/隧道已断。

而 2026-08-03 03:01 的自动化运行日志是这样收尾的：

```
[03:01:31] 数据库查询失败: [WinError 1225] 远程计算机拒绝网络连接。
[03:01:31]   ✅ E: E.资金闭环 - 成功
[03:01:36] F线运维检查完成: 端点 0/4正常
[03:01:36]   ✅ F: F.项目运维闭环 - 成功
[03:01:36] 全部闭环执行结束: 全部成功
```

**站点全挂、数据库拒连、SSH 超时，系统报告「全部成功」，且未向任何人发出一条通知。**

这不是「自动化程度不够」的问题，是**监控系统在提供虚假的安全感**——比没有监控更危险。你现在感觉项目在自动运行，实际它已经死了至少一天，而报告依然在按时生成、依然全绿。

在讨论「再加哪些定时任务」之前，必须先承认：**当前 0 个定时任务构成闭环。**

---

## 一、项目现状扫描（事实清单）

### 1.1 真实存在且质量不错的资产

| 资产 | 实测状态 |
|---|---|
| FastAPI 后端骨架 | 34 个 router、50 张表、132 个 py 文件，分层清晰 |
| 中医引擎 | 体质辨识、方剂、食疗、舌诊——真实实现，非占位 |
| 专业数据库对接 | UniProt / ClinVar(NCBI) / KEGG / STRING 均为真实 HTTP 调用，带 24h 缓存 + 限流 |
| 支付体系 | 虎皮椒（微信/支付宝）真实实现且密钥已配齐，含签名验签 |
| 增长体系 | Freemium(927行) + 积分(378行) + 阶梯邀请返佣(672行)，逻辑完整 |
| 测试 | 34 个测试文件、177 个 test 函数、3818 行 |
| GEO 基础设施 | llms.txt / ai.txt / robots.txt 端点——AI Agent 时代内容分发入口，有前瞻性 |
| 中医语料 | 701 个古籍 txt |
| 命名合规体系 | PRINCIPLES.md 的"禁止诊断/处方/治疗"规范，是明智的法律隔离 |

### 1.2 核心叙事与实际交付的落差

| 对外宣称 | 代码实测 |
|---|---|
| 五层因果链（L1基因→L5干预） | `fusion.py` **全部返回硬编码 DEMO_FUSION_CHAIN**，3 处 TODO 标注"认证用户仍读 demo" |
| Agnes AI 驱动分析 | `AGNES_API_KEY` 在 .env / .env.example / .env.production **三个文件全部缺失** → 恒定抛异常降级到 demo |
| 被动数据采集优先 | Withings / 小米 / 华为连接器均标 `Phase 1 mock`；Apple Health 直接 `NotImplementedError` |
| AlphaFold 结构预测 | **无任何 HTTP 调用**，只是 demo 里一个字符串常量 |
| SEO 内容 AI 生成 | `seo_factory.py`(1570行) 是纯模板拼装，无 LLM 参与 |
| 331 本古籍知识库 | 701 个 txt 静态文件，**未 RAG 化、未进入任何推理链** |

### 1.3 三个会让项目起不来的技术故障

**① Alembic 迁移双 head**
`002_add_missing_tables` 与 `002_add_sleep_repair_points` 的 `down_revision` 都是 `001_initial`，无 merge revision。而 `init-db` 容器执行的正是 `alembic upgrade head` → 直接报 `Multiple head revisions`。**这很可能就是站点 530 的直接原因。**

**② Celery Beat 根本没有部署**
docker-compose.yml 的服务列表：`web / worker / db / redis / minio / init-db`。worker 命令是 `celery worker --concurrency=2`，**无 `--beat`、无独立 beat 服务**。全项目 yml/sh/Makefile grep "beat" 结果为空。

> 结论：`app/worker.py` 里那 9 个定时任务，**从写下那天起就没有执行过一次**。

**③ 即使部署 beat，时间也全是错的（三重错误叠加）**

```python
crontab(hour=3, minute=0, day_of_week=2),  # 注释写"周三"
```
Celery 的 `day_of_week` 是 0=周日，`2` 实际是**周二**。作者按 Python `datetime.weekday()`（0=周一）的习惯写的，导致 6 个周任务系统性提前一天。

更隐蔽的是最后一行：
```python
celery_app.conf.beat_schedule_timezone = "Asia/Shanghai"
```
**`beat_schedule_timezone` 不是 Celery 的合法配置项**（正确是 `timezone`）。这行是空操作，beat 会按 UTC 运行 → **再偏 8 小时**。

所以实际执行时间 = 声明时间 − 1 天 − 8 小时。

| beat key | 代码真实执行 | task_registry 声明 |
|---|---|---|
| seo-generate-conditions | 周二 03:00 UTC = 周二 11:00 CST | 周三 03:00 |
| seo-generate-herbs | 周五 → 周五 11:00 CST | 周六 03:00 |
| user-education-push | 周四 → 周四 11:00 CST | 周五 03:00 |
| cleanup-old-analytics | 周六 → 周六 13:00 CST | 周日 05:00 |
| reactivate-deep-silent | 周一 → 周一 14:00 CST | 周二 06:00 |
| acquisition-weekly-report | 周日 → 周日 12:00 CST | 周一 05:00 |

**`task_registry.py` 自称"唯一真实来源"，但它不安装任何 crontab，也和真实配置对不上——它是一张与现实脱节的声明表。**

### 1.4 auto-pipeline 逐脚本真实性判定

| 阶段 | 判定 | 证据 |
|---|---|---|
| phase_1 collect | ❌ **纯硬编码** | intel_001~007 写死在源码，无网络请求。注释自认"实际生产中这里会调用爬虫/API" |
| phase_2 analyze | ✅ 真实 | 加权评分逻辑真实（但输入是假的） |
| phase_3 decide | ✅ 真实 | 去重、建队列 |
| phase_4 develop | ❌ **模板填充** | 一个 f-string 大模板，只替换 `{title}`，所有文章正文完全相同 |
| phase_5 test | ✅ 真实 | regex 扫医疗用语/Schema/字数 |
| phase_6 deploy | ❌ **永久 dry-run** | `deploy_config.get("dry_run", True)`，而 config.json **没有 dry_run 键** → 恒为 True，SCP 分支永不可达 |
| feedback_a | ❌ **伪造数据** | `hash(slug) % 350` 编造曝光/点击/排名 |
| B/C/D 线 | ❌ **全硬编码** | 函数名直接写"（模拟）" |
| E 线 finance | ✅ 真实 | asyncpg 查真实订单（但 DB 连不上） |
| F 线 ops | ⚠️ 半真 | curl 探测真实，但只检测不修复，且全挂仍报成功 |

**决定性证据：`auto-pipeline/content/generated/` 是空目录。** state 里声称已部署的 2 个 HTML 根本不存在。

### 1.5 运行状态实测

```
pipeline_id: W30-2026    created: 2026-07-27
A线6阶段全部 completed —— 但都在 17:04:40~41 的 1 秒内完成（本地假数据处理）
A线最近一次执行: 2026-07-27，此后再未运行（8-03 日志: "A线结束: 执行 0"）

development_tasks:
  task_edu_001  test_failed   ← 7月27日失败至今，无重试、无修复、无告警
  task_seo_002  deployed      ← dry-run 假部署
  task_seo_003  deployed      ← dry-run 假部署

deployment_history: 1 条，note 全是 "dry-run"
```

**`result_aggregator.py` 里 10 处硬编码 `"auto_fixable": False`，函数名叫 `extract_manual_actions`。代码本身承认：它只产出人工 TODO 列表，没有自愈能力。**

### 1.6 财务实测

```
总用户 0 · 付费用户 0 · 累计收入 ¥0
周成本 ¥77.5 · 周利润 -¥77.5 · 月成本约 ¥333
unit_economics: negative
```

---

## 二、四视角评估

### 2.1 用户视角

**打不开。** 这是当前唯一重要的用户事实，其余讨论都建立在假设站点能访问的前提上。

假设站点恢复后：

- **信任毁灭点**：核心的"五层因果链"对所有用户返回同一份 demo 数据。健康领域的用户会立刻发现"这不是我的分析"。一旦被发现，信任无法重建——健康产品的信任是一次性的。
- **数据入口断裂**：PRINCIPLES.md 把"被动数据采集优先"写为原则，但所有可穿戴连接器都是 mock。用户实际唯一能用的入口是 OCR 拍照上传报告——这恰恰是最重的交互，与设计原则完全相反。
- **产品设计本身是好的**："加法思维、不做减法"、"随时采纳随时忽略"——这是真正理解健康行为改变心理学的设计，比市面上打卡罚站式的健康 App 高明。但**从未被任何真实用户验证过**。
- **最致命的体验缺口——没有反馈回路**：用户按方案调理 3 个月后，指标有没有变化？系统没有任何 before/after 设计。而"量化追踪健康改善"是你自己写在 config.json 里的第一条核心目标。**没有这个，产品就只是一个高级的报告生成器，用户第二次就不会再来。**

### 2.2 领域专家视角（中医 + 精准健康）

- **理论命门在 L3→L4**：整个范式的独创性在于"从细胞通路客观数据反推中医证候"。这个映射的证据基础是什么？——现在代码里是 demo，说明这个映射**既未工程化，也未验证**。这是整个项目的知识产权核心，也是唯一无法被抄袭的部分，但它目前不存在。
- **701 本古籍是沉睡资产**：静态 txt，没有结构化、没有向量化、没有进入任何推理链。竞品拿到同样的公开古籍，三个月就能追平。**古籍本身没有壁垒，"古籍→通路→证候"的结构化映射才有。**
- **合规存在真实矛盾**：一边用 PRINCIPLES.md 严格规避"诊断/处方/治疗"，一边做 ClinVar 变异致病性分级和药物基因组学（PGx）。**基因解读在国内属于医疗器械/医疗服务灰区，PGx 指导用药更是明确的医疗行为。** 命名规避解决不了实质认定问题。
- **缺少安全护栏**：药食同源配方没有看到配伍禁忌、剂量上限、与在服西药相互作用的检查。举例：用户在服华法林，方案推荐当归/丹参——这是真实的出血风险。**这是领域专家和监管第一个会攻击的点，也是最容易出事故的点。**
- **建议**：把"疗效证据层"作为一等公民建进数据模型。中医循证化最稀缺的就是结构化的干预-结局配对数据。你有可能成为国内第一个持有这类数据集的团队——**这比五层因果链更有商业价值，也更容易被投资人理解。**

### 2.3 技术专家视角

**架构选型是专业的**：FastAPI + Postgres/TimescaleDB + Redis + Celery + MinIO，34 router / 50 表 / 177 测试。这不是玩具项目的骨架。

**但存在系统性的"声明与现实脱节"**，且模式高度一致：

1. 写了 beat_schedule，没部署 beat
2. 写了 task_registry 声称"唯一真实来源"，不安装 crontab 且与代码不符
3. 写了 deploy 阶段，永久 dry-run
4. 写了 deploy.yml，`REGISTRY` 是空注释占位，必然失败
5. 写了情报收集，返回硬编码
6. 写了效果追踪，用 hash 造数

**这是"文档驱动开发"的典型症状：文档层和汇报层的完成度远高于执行层。** 危险在于——你读文档得到的项目状态，和真实状态相差极大，而你的决策是基于文档做的。

**最严重的工程反模式：fail-open（失败开放）**

```python
try:
    真实操作()
except Exception:
    return 兜底假数据()      # ← 然后 complete_phase() 正常退出 0
```

异常被吞掉、替换成假数据、上报成功。这导致：
- DB 连不上 → 报"资金闭环成功"
- 站点全挂 → 报"运维闭环成功"

**监控系统的第一原则是：宁可误报，不可漏报。当前实现是反的。**

**其他技术债**：
- 双套调度系统（Celery + auto-pipeline）状态互不感知
- auto-pipeline 跑在你的 **Windows 本机**（日志里 `WinError 1225`、路径 `C:\Users\xing\Desktop`）。这意味着**你电脑关机 = 全部自动化停摆**。这与"不需要我参与"的目标直接冲突。
- `is_phase_ready()` 只认 `pending` 状态，一旦某阶段 `failed`，下次运行会**永久跳过**，不人工介入就再也跑不动。这是 task_edu_001 从 7/27 卡到今天的原因。

### 2.4 投资人视角

**硬数字：0 用户、0 收入、月净流出 ¥333、站点宕机、单位经济为负。**

如果我是投资人，会这样看：

- **叙事与交付的落差是最大的减分项**。白皮书、PRINCIPLES.md、生态蓝图、五层因果链——文档体系的成熟度显著高于产品。这在尽调中是危险信号，因为它暗示团队的精力分配可能失衡。
- **第一个 demo 就会崩**：我会要求用我自己的体检报告跑一遍。看到所有人返回同一份 demo 数据，尽调即刻结束。
- **商业模式选错了**：积分买断 + Freemium，无订阅。健康管理是长期陪伴生意，**无订阅 = 无 LTV 复利 = 无估值想象空间**。这类产品的估值逻辑建立在 ARR 和留存曲线上，一次性积分包给不了这个。
- **真正可估值的资产**（诚实说，确实存在）：
  1. 中医引擎（体质/方剂/食疗/舌诊）+ 50 表数据模型——真实且完整
  2. 支付+积分+阶梯邀请全链路——虎皮椒已通，是真能收钱的
  3. GEO 基础设施（llms.txt / ai.txt）——**这是最被低估的一块。AI Agent 时代内容分发入口的卡位，绝大多数团队还没意识到。**
  4. 701 本古籍（若结构化）
- **合规风险未定价**：基因解读 + PGx 的资质问题一旦被监管认定，整条产品线要重构。这在 TS 阶段会被要求做法律意见书。

**结论：当前不具备融资条件。** 但这不是能力问题，是顺序问题。

**融资前需要且仅需要达成三件事**：
1. 五层因果链跑通**一个真实用户**的真实数据（不是 demo）
2. **10 个真实付费用户**（不是 mock 订单）——证明有人愿意为此付钱
3. 站点连续 30 天可用率 > 99%——证明这是个生意，不是个 demo

有了这三样，同样的技术资产，估值逻辑完全不同。
