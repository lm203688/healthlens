# HealthLens 投资人视角系统提升方案

> 视角：多家机构投资人联合尽调 + 产品/医学/技术三角复核
> 日期：2026-08-05
> 基准：代码实测（`healthlens/app/**` 132 py / 50 表）、线上部署状态（Cloudflare Pages `uses_functions=True` 已确认）、`中医/` 古籍语料、既有评估文档
> 目的：市场竞品对标 → 平台价值评估 → 功能/数据/模型缺口 → 提升方案 → 多角色用户旅程 → 优先级路线图

---

## 执行摘要（给投资委员会的一页）

**我们怎么看这个项目。** HealthLens 的架构在消费级健康赛道里是稀缺的：它把 **分子机制（基因/蛋白/通路）** 、**中医证候（体质/辨证/方剂/食疗）** 、**频率修复医学** 三条通常被不同公司割裂的能力，熔铸进一条「L1 基因 → L2 蛋白 → L3 通路 → L4 证候 → L5 干预」的五层因果链。同时它在 `llms.txt / ai.txt` 上的 GEO 卡位、对 UniProt/ClinVar/KEGG/STRING 的真实数据库对接、以及已打通的真实支付（Creem + 迅虎，Functions 现已上线 `uses_functions=True`）都是真东西，不是 PPT。

**但投资人必须盯住三个硬事实：**
1. **信任命门仍开着。** `diagnosis_agent.py` 已写好真实 LLM 链路，但它被 `AGNES_API_KEY`（config 默认空字符串）卡死；一旦线上未配该 key，所有用户都会回退到**同一份 `DEMO_FUSION_CHAIN`**。遗留端点 `app/api/fusion.py` 至今仍直接吐 demo。在健康领域，第一个 demo 被识破，信任即归零且不可逆。
2. **ML 层是空的。** `app/ml/models/` 目录存在但无任何模型；诊断本质是「prompt 工程 + 外部数据库」，没有检索增强、没有训练、没有评测基准。L3→L4「分子机制→证候」的桥接在代码里还是占位字符串 `"待 AI 分析映射"`——这是全项目**唯一的不可抄袭 IP，却尚未工程化、未验证**。
3. **古籍是沉睡资产。** `中医/` 下 100+ 部古籍（神农本草经、本草纲目、千金方、伤寒论等）仍是裸 txt，未结构化、未向量化、未进入任何推理链；`tcm_knowledge / tcm_formula / tcm_syndrome / knowledge_graph` 模型已铺好落库位，但里面是空的。

**结论：** 资产值钱，顺序要对。当前**不具备融资条件**，但差距是「执行顺序」问题而非「能力」问题。本方案给出把「报告生成器」升级为「健康改善陪伴平台」、把「古籍」变成「独家结构化数据集」、把「一次性积分」变成「订阅+LTV」的具体路径。

---

## 一、投资人视角：市场与竞品对标

### 1.1 市场分层地图

| 层 | 代表玩家 | 数据维度 | HealthLens 相对位置 |
|---|---|---|---|
| 消费级 longevity / 精准健康 | InsideTracker、Function Health、Everest | 血检+基因 | 多了中医证候与频率医学层 |
| 可穿戴连续监测 | Whoop、Oura、Apple Watch、Garmin | 心率/睡眠/活动 | 仅连接器 mock，未形成闭环 |
| AI 症状/健康助手 | Ada Health、K Health、Qaly、Comet | 症状问答 | 缺纵向量化与中医 |
| DTC 基因 | 23andMe（衰退）、WeGene/微基因、Sequencing.com | 基因 | 基因注释真实，但 PGx 合规灰区 |
| 中医数字化 | **问止中医**、大经/脉景、小鹿中医（被收）、榕树家 | 辨证/处方 | 缺分子层与证据层 |
| 中国健康平台 | 京东健康、阿里健康、平安好医生、善诊 | 电商/问诊 | 缺个性化因果链 |
| 频率/再生医学 | （基本空白） | — | **蓝海，差异化主轴** |

### 1.2 对标结论（投资人读法）

- **问止中医** 是最接近的中医 AI 竞品，但它**没有分子层、没有 L1-L3**，本质是「名老中医经验数字化」。HealthLens 的「分子→证候」桥接若做成，就是降维打击。
- **InsideTracker / Function** 有分子层但**完全没有中医**，且是「单次报告」模式，无陪伴、无食疗、无频率修复。
- **Whoop/Oura** 有纵向数据但**无因果解释、无干预方案**，是「给你数字不给答案」。
- **真正的空白带**：「分子机制 × 中医证候 × 频率修复 × 纵向陪伴」四者融合，且把 100+ 古籍变成结构化证据底座——这条赛道目前**只有 HealthLens 在占**。

### 1.3 必须正视的差距与风险

- **合规灰区**：ClinVar 致病性分级 + 药物基因组（PGx）在国内属医疗器械/医疗服务认定边界；命名规避（PRINCIPLES.md 禁「诊断/处方」）解决不了实质认定。**TS 阶段会被要求法律意见书。**
- **商业模式错配**：积分买断 + Freemium，**无订阅**。健康是长期陪伴生意，无订阅 = 无 LTV 复利 = 无估值想象空间。
- **信任风险**：见执行摘要第 1 条，这是上线即爆的雷。

---

## 二、平台价值评估：真实资产 vs 缺口

### 2.1 经代码验证的真实资产（值钱的）

| 资产 | 代码证据 | 价值 |
|---|---|---|
| 五层因果链架构 | `diagnosis_agent.py` / `diagnosis_engine.py` | 差异化主线，稀缺 |
| 中医四件套 | `tcm_engine` / `tcm_food_therapy` / `tcm_formula_engine` / `tcm_tongue_analyzer` | 真实实现，非占位 |
| 频率/细胞修复医学 | `frequency_prescription.py`（497 行） | 对应白皮书，蓝海主轴 |
| 多源连接器 | `connectors/`：Apple/华为/Withings/小米/医院 LIS | 数据入口潜力 |
| 真实数据库对接 | `bio_database.py`：UniProt/ClinVar/KEGG/STRING（带缓存限流） | 专业可信 |
| 支付全链路 | `creem_pay_service` + `xunhu_pay_service` + Functions 已上线 | 真能收钱 |
| GEO 卡位 | `llms.txt / ai.txt / robots.txt` | 最被低估的 AI 分发入口 |
| 增长体系 | Freemium(927) + 积分(378) + 阶梯邀请(672) | 完整 |
| 古籍语料 | `中医/` 100+ 部 txt | 若结构化即成壁垒 |
| 数据模型底座 | 50 表含 `tcm_knowledge/formula/syndrome`、`knowledge_graph` | 结构化落库位已铺 |

### 2.2 缺口（分三层，逐条可验收）

**A. 功能缺口**
- **无纵向追踪（before/after）**：用户按方案调理 3 个月后，指标变没变？系统无前后对比设计。这是「用户第二次不来」的第一原因。
- **被动采集仍 mock**：`connectors` 多为 `Phase 1 mock`，Apple Health 甚至 `NotImplementedError`；用户唯一入口仍是 OCR 拍报告（最重交互），与「被动采集优先」原则相反。
- **安全护栏缺失**：药食同源/方剂无配伍禁忌、剂量上限、与在服西药相互作用检查（如华法林 + 当归/丹参 = 出血风险）。这是监管与专家第一攻击点。
- **结果不可导出/不可溯源**：缺 FHIR/PDF 一键导出给医生看。
- **无医生协作通道**：纯 C 端，缺专业背书闭环。

**B. 数据缺口**
- **古籍未结构化**：100+ 部裸 txt，零结构化、零向量化。
- **无干预-结局配对数据集**：中医循证最稀缺的就是「方案-结果」配对，HealthLens 有可能成为国内首个持有者——目前为空。
- **无真实诊断质量评估集**：LLM 诊断质量无 golden dataset、无自动评测。
- **无用户行为/留存数据**：0 真实用户，无法做任何增长建模。

**C. 模型算法缺口（最关键）**
- `app/ml/models/` **为空**：ML 层是占位。
- **无 RAG**：辨证不检索古籍/知识库，全靠 LLM 自由发挥。
- **L3→L4 桥接未工程化**：`tcm_bridge: "待 AI 分析映射"` 是占位，分子→证候映射无依据、无验证。
- **无纵向模型**：风险评分为快照（`risk_engine.py`），不随可穿戴/检验时间序列演化。
- **无评测基准**：综合判断/深度分析的质量不可度量。

---

## 三、模型与算法提升方案

> 目标：把诊断从「prompt 工程」升级为「检索增强 + 知识图谱 + 可量化不确定性的因果推理系统」。

**3.1 把「分子→证候」桥接从占位变成引擎（最高优先级 IP）**
- 现状：`tcm_engine.diagnose_syndrome` 是关键词匹配；`diagnosis_agent` 的 L3→L4 交给 LLM 自由生成（占位 `"待 AI 分析映射"`）。
- 方案：构建 **通路-证候映射知识图谱**（落到 `knowledge_graph.py` + `tcm_syndrome.py`），由「KEGG 通路异常模式 → 中医证候」的可溯源规则 + 古籍证据组成。让 LLM 的 L4 生成**引用该图谱与古籍**，而非凭空编。

**3.2 古医语料向量化 + 检索增强辨证（RAG）**
- 将第四章结构化后的实体/方剂/证候做 embedding 索引；辨证时先检索再生成，输出带来源引用（「据《本草纲目》…」）。直接消灭「幻觉辨证」。

**3.3 纵向时间序列模型**
- 用 `connectors` 接入的可穿戴/检验序列，训练轻量趋势模型（如分段线性 + 异常检测），动态重评 `risk_engine` 的风险分级，并驱动「复查提醒引擎」。

**3.4 不确定性量化**
- 现有 `confidence / evidence_level` 设计良好，但来源空。用 **贝叶斯式证据融合**：每条结论的 confidence = f(多源数据库命中数、古籍证据等级、纵向一致性)，让「证据不足」可被机器诚实表达。

**3.5 评测基准（质量可度量）**
- 建 golden dataset（100 例脱敏真实/合成病例，含专家标注的 L4 证候与 P0 动作）；用 **LLM-as-judge + 领域专家抽检** 自动评分「综合性判断 / 深度分析 / 可执行性」三维度。每次模型迭代跑基准，质量有曲线。

**3.6 轻量本地模型降本提隐私**
- 舌象/报告 OCR 与初诊可用本地 `qwen3.6`（已配置、支持视觉）跑，降低 API 成本、规避敏感数据出域，云端大模型只做深度综合。

---

## 四、古医多层次结构化数据工程（用户明确要求）

> 核心论点：**古籍本身没有壁垒，拿到公开古籍竞品三个月可追平；「古籍 → 通路 → 证候」的结构化映射才有壁垒。** 模型底座（`tcm_knowledge / tcm_formula / tcm_syndrome / knowledge_graph`）已铺好，只差把 100+ 部 txt 灌进去。

### 4.1 五层结构化架构

| 层 | 内容 | 落库模型 |
|---|---|---|
| **L0 文献层** | 书名/作者/朝代/版本/卷条目 | `tcm_knowledge`（source 字段） |
| **L1 实体层** | 药材、方剂、证候、症状、经络、穴位、治则 | `tcm_formula` / `tcm_syndrome` / `tcm_knowledge` |
| **L2 关系层** | 药材-功效、方剂-组成-主治、证候-症状、方剂-证候、药材-禁忌（十八反十九畏） | `knowledge_graph`（边表） |
| **L3 证据层** | 古籍出处、现代研究、证据等级（高/中/低） | `tcm_knowledge.evidence` |
| **L4 应用层** | 映射到 L3 通路、当代可操作食疗/频率方案 | `tcm_food_therapy` / `frequency_prescription` |

### 4.2 工程 Pipeline（可落地）

```
古籍 PDF/txt → 清洗(去噪/分卷) → LLM 抽取实体与关系(批处理)
            → 人工校验抽样(每部抽 5%) → 写入 tcm_* / knowledge_graph
            → embedding 索引(供 RAG) → 持续增量(新书自动入湖)
```

### 4.3 商业价值

- **壁垒**：独占的「分子-证候-古籍」结构化映射，是问止中医们没有的。
- **证据资产**：结构化后可沉淀「干预-结局」配对（结合 3.5 基准），成为国内首个中医循证数据集——**比五层因果链本身更易被投资人理解、更易变现（科研/保险/医院合作）**。
- **合规护身**：每个推荐可溯源到古籍 + 现代证据等级，天然支撑「非诊断、仅供养生参考」的合规叙事。

---

## 五、核心诊断与分析能力增强路线

> 直接对应 `diagnosis_agent.py` 与 `tcm_engine.py` 的现状。

**5.1 综合性判断（Comprehensive Judgment）增强**
- 新增 **跨层矛盾检测器**：自动对账 L1-L5（如基因显示 APOE ε4 高风险，但用户检验血脂正常、无症状 → 显式标注「矛盾/缺口」），不再只靠 LLM 自觉。
- `dominant_chain` 由「图谱驱动」生成：从 `knowledge_graph` 取主导因果主线，保证可复现。

**5.2 深度分析（Deep Analysis）增强**
- `evidence_level / confidence` 的来源从「空」变为「图谱+古籍+RAG 命中」；每条 L4/L5 带来源引用。
- `tcm_bridge` 从占位 `"待 AI 分析映射"` 改为由 `tcm_engine` + 古籍 KG 生成的**可溯源桥接文本**。

**5.3 闭环（Actionable + 反馈回路）**
- 加入 **before/after 对比模块**：用户第 2 次录入时自动比对首次，生成「改善/持平/恶化」结论（直接补上「用户不回头」的洞）。
- **复查提醒引擎**：基于纵向模型输出「何时复查 / 什么信号就医」。

**5.4 诊断质量 SLA**
- 真实 LLM 链路必须常驻（配 `AGNES_API_KEY`，并删除 `fusion.py` 的 demo 直吐路径）；任何回退必须显式打标 `is_demo=true` 并前端强提示，杜绝「假装真实」。

---

## 六、用户全流程走查（多角色）

> 流程统一为：注册 → 录入数据（基因/检验/OCR/中医问卷）→ 得五层报告 → 执行方案（食疗/频率/运动）→ 复查对比。逐角色找改进点。

### 角色 1：谨慎型中老年（养生/慢病，家人代操作）
- **走查**：被子女拉来用；看不懂英文指标；怕被「诊断」；最重交互是拍体检报告。
- **改进点**：① OCR 拍报告后给「大字号白话版」解读，禁用术语；② 报告首页先给「整体体质一句话 + 3 件可立刻做的小事」，而非满屏分子链；③ 强信任标识（「本结果由 XX 数据库 + 古籍佐证，仅供参考，不作为医疗诊断」）；④ 家属代管账号 + 一键分享给子女/社区医生。

### 角色 2：年轻量化族（wearable/数据控）
- **走查**：戴 Whoop/Oura，想要自己的数据；爱深挖；讨厌被当成小白。
- **改进点**：① 开放 **API / 数据导出（FHIR/CSV）**；② 纵向趋势图（自己的指标随时间的因果演化）；③ 「专家模式」可展开 L1-L5 全链与证据引用；④ 可自建实验（A/B 食疗 4 周看指标）。

### 角色 3：中医爱好者 / 自律养生者
- **走查**：信中医，想看依据，要方子能落地。
- **改进点**：① 每条推荐**附古籍出处 + 现代研究等级**（呼应第四章）；② 体质追踪随时间变化曲线；③ 食疗方给「买得到、做得出」的具体量（克数/做法/周期）；④ 频率修复方案可跟练（音频/计划表）。

### 角色 4：慢病家属（代管父母）
- **走查**：远程关心父母；要提醒、要可分享。
- **改进点**：① 多被监护人面板；② 异常自动提醒（子女+父母双端）；③ 报告可一键生成 PDF 发给父母的主治医生；④ 用药冲突预警（父母在服西药 vs 推荐方剂的相互作用）。

### 角色 5：专业用户 / 投资人 / 医生
- **走查**：要证据、要透明、要可追溯。
- **改进点**：① 证据面板（每条结论的证据等级 + 来源链接）；② 完整 FHIR 导出；③ 方法学透明度页（数据来源、模型版本、局限声明）；④ 诊断质量基准公开（可信度背书）。

### 跨角色共性改进
- **反馈回路**（before/after）是所有角色的留存关键。
- **移动端优先**：当前前端在 `healthlens/frontend` 与 `auto-pipeline/dist` 两套，需统一为移动友好的主流程。
- **信任可视化**：证据等级、数据缺口、不确定性要「看得见」，而非藏在 JSON 里。

---

## 七、提升优先级路线图（直接驱动平台价值/估值）

### P0（0–90 天，融资前必须）
| 动作 | 对应价值驱动 |
|---|---|
| 配 `AGNES_API_KEY` + 下线 `fusion.py` demo 直吐，确保诊断**对真实用户出真实结果** | 消除信任雷，达标「真实诊断」 |
| 纵向追踪 MVP（before/after 对比 + 复查提醒） | 把「报告器」变「陪伴平台」，拉留存 |
| 古医结构化 POC：先结构化 2–3 部核心（本草纲目/神农本草经/食疗本草）+ 高频食材方剂 | 验证壁垒可建，产出首个结构化数据集 |
| 安全护栏：方剂/食疗 vs 在服西药相互作用 + 十八反十九畏校验 | 消解监管与专家第一攻击点 |
| 信任标识 + 证据可视化（前端强提示 + 来源引用） | 合规护身 + 转化 |
| 目标：**10 个真实付费用户 + 30 天可用率 > 99%** | 融资前硬门槛 |

### P1（3–6 月）
- RAG 辨证（古籍向量化全量接入，消灭幻觉）
- 古医全量结构化 + `knowledge_graph` 灌满
- **订阅制上线**（替代纯积分买断，建立 ARR/LTV）
- 医生协作通道 + FHIR 导出
- 诊断质量基准（golden dataset + LLM-judge）常态化

### P2（6–12 月）
- 纵向时间序列模型驱动动态风险重评
- 多组学扩展（宏基因组/代谢组）
- 频率医学临床证据积累（干预-结局配对数据集）
- GEO 内容分发规模化（llms.txt 驱动的 AI 流量入口）

---

## 八、结论：如何提升平台价值

1. **从「报告生成器」→「健康改善陪伴平台」**：靠纵向追踪 + 复查闭环 + 订阅，建立留存与 LTV 复利。
2. **从「古籍文件」→「独家结构化数据集」**：五层结构化 + 干预-结局配对，是问止中医们追不上的壁垒，也是最容易变现/讲给投资人听的资产。
3. **从「一次性积分」→「订阅 + LTV」**：健康管理是长期生意，估值逻辑建立在 ARR 与留存曲线上。
4. **从「demo 信任风险」→「真实 + 透明」**：配齐 key、下线 demo 直吐、证据可视化，先活过「第一个真实用户」。
5. **把最稀缺的 IP 工程化**：L3→L4「分子→证候」桥接从占位变引擎，这是全项目唯一不可抄袭的部分。

**融资前 Checklist（复用既有三点 + 本方案新增）：**
- [ ] 五层因果链跑通 **1 个真实用户**的真实数据（非 demo）
- [ ] **10 个真实付费用户**（非 mock）
- [ ] 站点连续 **30 天可用率 > 99%**
- [ ] 古医结构化 **POC 数据集** 产出（≥ 1 部核心古籍 + 高频食材方剂入库）
- [ ] 安全护栏（西药相互作用 + 配伍禁忌）上线
- [ ] 诊断质量基准跑通（golden dataset + 自动评分）

> 附：线上支付 Functions 已于 2026-08-05T10:31Z 部署 `4bbb278f` 确认 `uses_functions=True` 上线；但诊断真实链路仍受 `AGNES_API_KEY` 配置状态制约，需在 P0 立即确认线上 env 已配。
