# HealthLens 自动化补位方案

> 配套文档：`HealthLens_全面评估_2026-08-04.md`
> 目标：从"定时执行"升级到"无人值守自运行"

---

## 三、自动化成熟度分级：你现在在哪一级

| 级别 | 定义 | HealthLens 现状 |
|---|---|---|
| L0 手动 | 人工触发 | — |
| L1 定时执行 | 按时跑，产出报告 | ← **auto-pipeline 在这里** |
| L1.5 **虚假绿灯** | 跑了，但失败被吞成成功 | ← **实际在这里，比 L1 更差** |
| L2 定时 + 告警 | 失败会通知到人 | 缺失（0 个告警通道） |
| L3 **自动修复 + 复验** | 失败自动处置并验证 | 缺失 |
| L4 自我优化 | 根据结果调整策略 | 缺失 |
| — | Celery Beat 的 9 个任务 | **L0（从未运行）** |

**"不需要我参与" = L3。从 L1.5 到 L3，中间隔着两级，而且必须按顺序补。**

---

## 四、五大维度覆盖度评估

### 维度一：项目自动化监测修复

| 环节 | 现状 |
|---|---|
| 探测 | ⚠️ F线 curl 4 端点 + ssh docker ps —— 真实，但**失败仍报成功** |
| 判定 | ❌ 无阈值、无连续失败判定、无分级 |
| 执行 | ❌ 只写一句 `"action": "修复端点: xxx"` 到 JSON |
| 复验 | ❌ 无 |
| 升级 | ❌ 无任何告警通道（无邮件/无webhook/无短信） |

**闭环度 1/5。** 实证：站点已挂 ≥1 天，你到现在才知道。

**结构性缺失**：
- **零备份**。全项目无任何数据库备份任务。服务器一旦损毁，50 张表 + 全部用户数据归零。这是当前最大的隐性风险，甚至超过站点宕机。
- 无磁盘/内存水位监控
- 无 SSL 证书到期监控
- 无日志错误聚合

### 维度二：自动推广运营功能迭代

| 环节 | 现状 |
|---|---|
| 生成 | ⚠️ 模板拼装（A线 develop 所有文章正文相同；Celery seo_factory 未运行） |
| 测试 | ✅ phase_5 真实（唯一真正工作的环节） |
| 上线 | ❌ **永久 dry-run，从未真正部署过一个文件** |
| 复验 | ❌ 无部署后 URL 200 校验 |
| 效果 | ❌ feedback_a 用 `hash(slug)` 伪造曝光/点击/排名 |

**闭环度 1/5。**

**断口实证**：`reports/seo-import-queue.json` 里 14 个页面 `status: "pending_import"`，生成于 7/31，**至今无任何代码读取它**——生产了，没有消费者。

**缺失**：搜索引擎主动推送（IndexNow/百度）、收录率回查、真实排名 API、多渠道分发（知乎/小红书/公众号）。

### 维度三：情报收集分析采纳迭代

| 环节 | 现状 |
|---|---|
| 收集 | ❌ **7 条硬编码常量**，无网络请求 |
| 分析 | ✅ 加权评分逻辑真实 |
| 决策 | ✅ 阈值门禁真实 |
| 采纳 | ⚠️ 生成任务队列，但下游 develop/deploy 是假的 |
| 迭代 | ❌ 权重从未根据效果调整（因为效果数据是伪造的） |

**闭环度 2/5。** 这是最讽刺的一环：**分析和决策逻辑写得很认真，但输入是虚构的，输出无人执行。** 一台精密的机器在空转。

**缺失**：真实爬虫（GitHub/arXiv/PubMed 都有免费 API）、竞品价格功能 diff 监测、**政策法规监测**（药监局/卫健委/中医药管理局——对你这个赛道是生死线，现在完全没有）。

### 维度四：技术迭代用户反馈升级

| 环节 | 现状 |
|---|---|
| 反馈采集 | ❌ **0 用户 = 无反馈源** |
| 埋点聚合 | ❌ B线转化数据全硬编码（"7日留存42.3%"是写死的） |
| 问题识别 | ❌ 无 |
| 自动迭代 | ❌ 无 |

**闭环度 0/5。** 这一维度当前**不成立**——没有用户就没有反馈，没有反馈就没有迭代依据。

**关键判断**：这个维度不应该现在补自动化任务，应该先解决"有用户"。为 0 用户建反馈自动化是浪费。

### 维度五：专业数据库对接与自建库扩展

| 环节 | 现状 |
|---|---|
| 外部库对接 | ✅ UniProt/ClinVar/KEGG/STRING 真实（AlphaFold 是假的，4/5） |
| 增量同步 | ❌ 无。ClinVar 每月发版，你的数据永远停在首次查询 |
| 自建库扩展 | ❌ C线"1063200条记录、0个质量问题"是**硬编码**，无真实 SQL |
| 语料资产化 | ❌ 701 本古籍未 RAG 化 |
| 数据质量巡检 | ❌ 无 |

**闭环度 1.5/5。**

### 汇总

| 维度 | 闭环度 | 结论 |
|---|---|---|
| 一 · 监测修复 | 1/5 | 会撒谎的监控 + 零备份 |
| 二 · 推广运营 | 1/5 | 永久 dry-run |
| 三 · 情报迭代 | 2/5 | 精密机器空转 |
| 四 · 反馈升级 | 0/5 | 不成立（无用户） |
| 五 · 数据库 | 1.5/5 | 对接真实，无同步无扩展 |
| **整体** | **1.1/5** | **不构成自动化闭环** |

---

## 五、补位任务清单

### 铁律：先修地基，再加任务

**在断裂的地基上加定时任务，只会产出更多虚假报告。**

### P0 — 地基修复（不做这些，后面全部无效）

| # | 动作 | 说明 |
|---|---|---|
| F1 | 合并 alembic 双 head | `alembic merge -m "merge" <rev1> <rev2>`。这很可能是站点 530 的直接原因 |
| F2 | 恢复站点 | 修 F1 后重启栈，验证 4 端点 200 |
| F3 | **docker-compose 增加 beat 服务** | `celery -A app.worker beat -l info`。地基中的地基——没有它，9 个任务永远不跑 |
| F4 | 修 `day_of_week` 偏移 | 全部 +1（Celery 0=周日） |
| F5 | 改 `beat_schedule_timezone` → `timezone` | 当前是无效配置项，会按 UTC 跑 |
| F6 | **fail-open 改 fail-loud** | 所有 `except: return 假数据` 改为 `fail_phase() + 非零退出`。**这一条比新增任何任务都重要** |
| F7 | 接入告警通道 | Server酱 / 企业微信机器人 / 邮件，任选其一，10 分钟能配好 |
| F8 | 关掉 dry-run | config.json 显式写 `"dry_run": false` |
| F9 | 配 `AGNES_API_KEY` 或换本地模型 | 你本机有 Ollama qwen3.6:36B，可零成本替代 |
| F10 | **auto-pipeline 迁到服务器** | 现在跑在你的 Windows 本机，关机即停摆，与"无人值守"直接冲突 |

> **F6 单独强调**：现在的系统会把失败包装成成功。在修好这一条之前，你看到的每一份报告都不可信，包括未来新增任务产出的报告。

### P1 — 自愈闭环（维度一，真正的"不需要你参与"）

每个任务按 **探测→判定→执行→复验→升级** 五段式设计。

| # | 任务 | 频率 | 闭环设计 |
|---|---|---|---|
| **T1** | 站点存活自愈 | 每 5 分钟 | 探测 4 端点 → 连续 3 次失败 → SSH `docker compose restart web` → 60s 后复验 → 仍失败则告警并升级到 T2 |
| **T2** | 容器级自愈 | 每 10 分钟 | `docker ps` 检查 → 发现 exited → 自动 restart → 复验 → 连续 2 次重启失败则**回滚到上一镜像 tag** → 告警 |
| **T3** | **数据库备份 + 恢复验证** | 备份每日 02:00 / 验证每周日 | `pg_dump` → 上传 MinIO/COS → **每周恢复到临时库并跑 count 校验** → 失败告警。**没有验证过的备份等于没有备份** |
| **T4** | 资源水位自愈 | 每小时 | 磁盘>85% → 自动清理 7 天前日志 + `docker image prune` → 复验 → 仍超阈值告警 |
| **T5** | SSL 证书哨兵 | 每日 | 剩余<15 天 → 自动 certbot renew → 复验有效期 → 失败告警 |
| **T6** | 错误日志→自动修复 | 每 6 小时 | 聚合 5xx/异常 → 相同错误>10 次 → 调本地 Ollama 生成补丁 → **跑 pytest** → 通过则开 PR（初期人工 merge，稳定后自动） |
| **T7** | **心跳看门狗（Dead Man's Switch）** | 每 30 分钟 | 检查每个任务是否在预期窗口内上报心跳 → 缺失即告警。**这一条专治"任务死了但没人知道"，是整个方案里性价比最高的** |

### P2 — 发布闭环（维度二）

| # | 任务 | 频率 | 闭环设计 |
|---|---|---|---|
| **T8** | 真实部署 + 回滚 | 事件驱动（test 通过后） | SCP 上传 → **curl 复验 200 + 内容校验** → 失败自动删除文件并恢复 sitemap → 告警 |
| **T9** | 搜索引擎主动推送 | 每日 | 新页面推 IndexNow(Bing) + 百度 API → 记录推送结果 → 7 天后回查收录 → 未收录则重推 |
| **T10** | 真实排名追踪 | 每周 | 替换 `hash()` 伪造 → 真实查询 → 排名连续下降 → **自动生成内容更新任务回灌 A 线** |
| **T11** | 多渠道分发 | 每周 | 一篇内容自动改写为知乎/小红书/公众号版本 → 发布 → 回收各渠道数据 → 反哺渠道权重 |
| **T12** | 死链与索引巡检 | 每周 | 扫 sitemap 全部 URL → 404/500 自动从 sitemap 摘除 → 建修复任务 |

### P3 — 情报闭环（维度三）

| # | 任务 | 频率 | 闭环设计 |
|---|---|---|---|
| **T13** | 真实情报采集 | 每周一 | 替换硬编码 → GitHub Trending API + arXiv API + PubMed E-utilities（全免费）→ 采集量为 0 时告警（防静默失败） |
| **T14** | 竞品变更监测 | 每周 | 抓竞品定价页/功能页 → **diff 对比** → 有变化才告警（无变化不打扰） |
| **T15** | **政策法规哨兵** | 每日 | 监测药监局/卫健委/中医药管理局 → 命中"基因检测/健康管理/中医药服务"关键词 → **高优先级告警**。⚠️ 你的赛道合规风险高，这是唯一建议"必须打扰你"的任务 |
| **T16** | 决策权重自学习 | 每月 | 用 T10 真实效果数据回归 → 自动调整 config.json 的四项权重 → 记录版本可回滚。**这是 L4 自我优化的入口** |

### P4 — 数据资产闭环（维度五）

| # | 任务 | 频率 | 闭环设计 |
|---|---|---|---|
| **T17** | 专业库增量同步 | 每月 | 检查 ClinVar/KEGG/UniProt 版本 → 有更新则增量拉取 → **diff 出受影响的用户方案** → 触发重算 + 通知受影响用户 |
| **T18** | **古籍语料 RAG 化** | 一次性 + 增量 | 701 txt → 分句结构化 → 抽取"药材-性味-归经-主治-配伍" → 向量化入库 → 接入方案生成。**这是把沉睡资产变成壁垒的唯一路径** |
| **T19** | 数据质量真实巡检 | 每日 | 替换硬编码 → 真实 SQL 查空值率/孤儿外键/时间戳异常 → 可自动修复的直接修 → 其余建工单 |
| **T20** | **疗效数据回收** | 每周 | 提取用户 before/after 指标配对 → 沉淀为证据库 → 统计有效率 → 反馈优化配方权重。**这是数据飞轮的真正引擎，也是最有价值的缺失项** |

### P5 — 元自动化（管自动化的自动化）

| # | 任务 | 频率 | 说明 |
|---|---|---|---|
| **T21** | 任务健康度自检 | 每日 | 逐个检查：是否按时跑了 / 产出文件是否非空 / 耗时是否异常（1 秒跑完 6 阶段 = 假数据信号）/ 是否有静默失败 |
| **T22** | 卡死任务自动解锁 | 每日 | 扫 `failed_*` / 中间态（如当前的 `running_ops_health_f`）→ 自动重试一次 → 仍失败则 reset 并告警。**专治 task_edu_001 卡 7 天无人管** |
| **T23** | 周度真实性审计 | 每周 | 抽查报告数据是否来自真实数据源（检测 `data_source` 字段、检测 hash 伪造特征）→ 发现造假立即告警 |

### 维度四（用户反馈）—— 建议暂缓

**0 用户时建反馈自动化是纯浪费。** 等到有 50+ 真实用户再补 T24-T26：埋点漏斗自动分析、NPS 自动分类进开发队列、A/B 实验自动裁决。

---

## 六、执行顺序

```
第 1 周   F1-F10 地基修复
          → 验收：站点 200、beat 在跑、故意制造失败能收到告警
第 2 周   T7 心跳看门狗 + T1/T2 自愈 + T3 备份
          → 验收：手动 kill 容器，5 分钟内自动拉起
第 3 周   T8 真实部署 + T9 推送 + T21/T22 元自检
          → 验收：一篇内容全自动上线并被搜索引擎收录
第 4 周   T13 真实情报 + T15 政策哨兵 + T19 数据质量
第 2 月   T18 古籍 RAG 化 + 打通 fusion 真实数据链路
第 3 月   T20 疗效回收 + T16 权重自学习 → 进入 L4
```

---

## 七、无人值守的判定标准

达成 L3 的验收方式——**做这三个破坏性测试，全部通过才算数**：

1. **拔电测试**：手动 `docker stop web`，5 分钟内自动恢复，你只收到一条"已自愈"通知
2. **静默测试**：手动停掉某个定时任务，30 分钟内收到"任务失联"告警
3. **诚实测试**：手动断开数据库，系统必须报**失败**而不是"全部成功"

**第 3 条现在必定不通过——这是整个项目自动化的第一块多米诺骨牌。**

---

## 八、给你的三句话

1. **先让站点活过来**，其他都是空谈。
2. **修 fail-open 比加 20 个新任务都重要**——一个会撒谎的系统，加再多任务只是产生更多谎言。
3. **T7 心跳看门狗 + T3 备份验证**，是投入产出比最高的两个任务。前者让你不再"最后一个知道"，后者防止你某天一无所有。
